#include<stdio.h>
int main()
{
    int n,d;
    printf("enter the value");
    scanf("%d",&n);
        if(n!=0)
        {
            d=n%10;
            printf("the value is %d",d);
        }
     return 0;
}
