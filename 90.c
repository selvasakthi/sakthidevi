#include <stdio.h>
int main()
{
    char a[100];
    int i;
    printf("enter the string");
    scanf("%s",&a[i]);
    for(i=0;a[i]>=5;i++)
   {
        if((a[i]>='0')&&(a[i]<='9'))
        {
            printf("%c",a[i]);
        }
    }
}
