#include <stdio.h>
#include <string.h>
void main()
{
    char sa[100];
    int count = 0,k;
    printf("enter the string\n");
    scanf("%[^\n]a",a);
    for (i = 0;a[i] != '\0';i++)
    {
        if (a[k] == ' ')
            count++;    
    }
    printf("number of words in given string are: %d\n", count + 1);
}
